# Gradle / build outputs
.gradle/
build/
app/build/

# Machine-specific SDK path (regenerated by Android Studio / local setup)
local.properties

# IDE
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# The tunnel core AAR (~107 MB) — same policy as desktop/resources/bin: too
# large for git and reproducible from fetch-core.ps1, which verifies the
# SHA256 pinned in core-hashes.json.
app/libs/*.aar
