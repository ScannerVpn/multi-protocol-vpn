# AUDIT — ۳۱ اوت ۲۰۲۶ (دور ۴: بازبینی دور پینگ سریع 3.6.12)

> **وضعیت (به‌روزرسانی دور ۵، همان روز):** P1-1 و P2-2 و هر پنج خرده‌فیکس P3 **در نسخه 3.6.13
> فیکس و با تست قفل شدند** (`AuditRegressionTest`: سمیافور، precheck TCP-محور، سقف پورت ephemeral).
> وضعیت دقیق در `PLAN.md` §۸ (✅ فیکس‌های آدیت). این سند به‌عنوان سابقه‌ی تحلیل حفظ شده است.

بازبینی کد دور ۳ (پینگ سریع) + اجرای واقعی تست‌ها. دامنه: `VpnPing.kt`، `TrafficProbe.kt`،
`Ports.kt`، `Xray.kt`، `Vpn.kt` (مسیرهای پینگ/کانکت)، `AppState.kt`/`HomeScreen.kt`/`ServersScreen.kt` (UI پینگ)،
`AuditRegressionTest.kt`.

**وضعیت تأییدشده با اجرا:** `gradlew --offline test` → **۲۰۵ تست / ۰ شکست / ۰ خطا**
(فایل‌های result در `desktop/build/test-results/test` جدیدتر از همه‌ی سورس‌ها هستند).
سایز EXE بیلد قبلی: `build/compose/binaries/main/app/MultiVPN/MultiVPN.exe` (۳۰ اوت).

## تأیید قراردادهای PLAN.md (همه برقرار — با کد چک شد)

1. **عدد پینگ فقط از ترافیک واقعی:** `TrafficProbe.isRealNoContent` فقط 204 یا 200-بدون-بدنه را قبول می‌کند؛ redirect رد می‌شود؛ HTTPS-first با ۴ endpoint مسابقه‌ای. ✓
2. **kill هدفمند-PID:** racer‌ها فقط `Xray.killPid(myPid)` صدا می‌زنند؛ `killCommands(pid, sys)` مسیر image-wide را فقط وقتی pid نامعلوم است باز می‌کند و `kill()` هرگز `lastPid` را خراب نمی‌کند. ✓
3. `VpnPing.setSessionLive` از connect/disconnect/abort/killAllCores mirror می‌شود. ✓
4. بودجه‌ها (2000/3000/4000) با تست reflection قفل شده‌اند. ✓
5. UI تک‌فکتی: StatCard/ConfigStrip/DashboardFooter حذف و `SessionFactsRow` جایگزین شده. ✓

## یافته‌ها (به ترتیب اهمیت)

### P1-1 — سقف `PARALLEL = 8` فقط روی کاغذ است؛ سقف واقعی ۲۴ و برای لیست‌های بزرگ باگ عدد-پاک‌کن دارد

`PARALLEL = 8` در `VpnPing` تعریف شده ولی **هیچ Semaphore/limitری در کد نیست** —
`pingAllConfigs` همه‌ی کانفیگ‌ها را همزمان launch می‌کند و `quickXrayPing` بدون هیچ دروازه‌ای core می‌سازد.
تنها سقف واقعی، پول ۲۴تاییِ پورت scratch است. دو پیامد:

- **لیست >۲۴ ردیف:** ردیف‌های ۲۵+ در همان موج اول `claimScratchPorts() = null` می‌گیرند →
  `Skipped` فوری → و `AppState.pingConfig` در شاخه‌ی Skipped **عدد قبلی ردیف را پاک می‌کند**
  (`latency = latency - config.id`). یعنی پینگ‌گرفتن یک لیست بزرگ عملاً نصف لیست را به «—» تبدیل می‌کند
  تا وقتی دوباره دستی پینگ بگیری.
- به‌جای ۸ هسته، تا **۲۴ پروسه‌ی xray + ۹۶ نخ probe** همزمان بالا می‌رود (هر racer یک pool ۴تایی
  TrafficProbe دارد) — روی ماشین ضعیف spike می‌دهد.

**فیکس پیشنهادی (کوچک و کم‌ریسک):** `private val racerGate = Semaphore(VpnPing.PARALLEL)` و
دور بدنه‌ی `quickXrayPing` (بعد از گاردهای session/رunning و قبل از claim پورت) `withPermit`.
با این کار صف‌بندی درست می‌شود، پول هیچ‌وقت خالی نمی‌شود (۸ ≤ ۲۴) و Skipped-فوری برای
ردیف‌های اضافه حذف می‌شود. تست `scratch ports never collide` دست‌نخورده می‌ماند.

### P2-2 — TCP precheck برای پروتکل‌های UDP-محض جواب جعلی «timeout» می‌سازد

`quickXrayPing` و `quickHysteriaPing` قبل از ساخت core، `tcpLatency(address, port)` را اجرا می‌کنند و
شکستش = `RealPingResult.Failed` (در UI: قرمز «timeout»). اما:

- **hysteria2 روی QUIC/UDP است** — سروری که TCP روی آن پورت گوش نمی‌دهد (رایج) کاملاً سالم است ولی
  precheck می‌بَرد و UI مرگ دروغین نشان می‌دهد. این دقیقاً همان دروغی است که قرارداد honesty می‌خواهد نگوید.
- در `buildClientJson` ترنسپورت‌های `kcp` و `quic` هم پشتیبانی می‌شوند — برای آن لینک‌ها precheck TCP بی‌معناست.

**فیکس پیشنهادی:** precheck را فقط برای networkهای TCP-محور (tcp/ws/grpc/httpupgrade/xhttp/h2) اجرا کن؛
برای hysteria2/kcp/quic یا کلاً ردش کن (مستقیم برو سراغ تست core)، یا فقط «connection-refused سریع» را
fail بگیر و «drop/timeout» را ادامه بده.

### P3-3 — عدد ICMP سرور از همان `LatencyPill` پینگ ترافیکی نشان داده می‌شود

دکمه‌ی Ping در تب Servers عدد `Test-Connection` (ICMP خام) را از همان کامپوننت
`LatencyPill` که عدد ترافیک end-to-end را نشان می‌دهد، می‌چسباند. از دید بند ۱ قرارداد
(«هیچ ICMP به‌عنوان پینگ نمایش داده نمی‌شود») این نمایش استثناست. اگر عمدی است (فقط diagnostic سرور)،
یک pill جداگانه با لیبل «ICMP» یا رنگ خنثی بگذار تا دو معنی برای یک شکل بصری نباشد.

### P3-4 — دو آستانه‌ی رنگی متفاوت برای یک فکت

`PingChip` در HomeScreen: سبز <80 / کهربایی <200 / قرمز.
`LatencyPill` در Components: سبز <150 / کهربایی <400 / قرمز.
همین عدد در دو صفحه دو رنگ می‌شود. یکی‌شان کن (ترجیحاً از `LatencyPill` استفاده کن تا فکت یک‌جا باشد).

### P3-5 — خرده‌نکات (بی‌ضرر ولی عرضی)

- `claimScratchPorts` در مسیر «اسلات اشغال بود» timestamp کلیمِ دیگری را با `now` رفرش می‌کند
  (TTL کلیم‌های زنده را کمی جابه‌جا می‌کند؛ چون release همیشه remove می‌کند، درست کار می‌کند — فقط عرضی است).
- در `raceEndpoints` بعد از برنده، بازنده‌ها تا سقف timeout زنده می‌مانند (نخ‌های daemon؛ خروج اپ را بلاک نمی‌کنند).
- اگر کاربر base port را نزدیک ۶۵۰۰۰ بگذارد، پورت‌های scratch وارد بازه‌ی ephemeral ویندوز (49152+) می‌شوند
  و یک کانکشن تصادفی می‌تواند پورت را قبل از core بگیرد. کف امن: `sanitize` را به `MIN..49151` محدود کن یا
  scratch را زیر 49152 نگه دار.
- `HANDOFF.md` هنوز می‌گوید «۱۲۳ تست» — الان ۲۰۵ است (PLAN.md درست است). *(دور ۵: اصلاح شد — ۲۰۸.)*

## جمع‌بندی

معماری دور ۳ سالم است و قراردادها واقعاً در کد برقرارند (نه فقط در داکیومنت). دو فیکس P1-1 و P2-2
هر دو کوچک‌اند (هرکدام <۳۰ خط + تست) و ارزش انجام در دور بعد را دارند؛ بقیه نمایشی/دокументی‌اند.
